import {createClient} from 'https://esm.sh/@supabase/supabase-js@2';
const h={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, apikey, content-type','Content-Type':'application/json'};const out=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:h});const digest=async(v:string)=>Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v))),b=>b.toString(16).padStart(2,'0')).join('');
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:h});if(req.method!=='POST')return out({ok:false},405);const url=Deno.env.get('SUPABASE_URL')!,key=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,pepper=Deno.env.get('CONTACT_RATE_LIMIT_PEPPER');if(!url||!key||!pepper||pepper.length<32)return out({ok:false,error:'Configuração segura incompleta.'},500);const ip=req.headers.get('cf-connecting-ip')||req.headers.get('x-forwarded-for')?.split(',')[0]||'unknown',ipHash=await digest(`${pepper}:contact:${ip}`),db=createClient(url,key);const since=new Date(Date.now()-60*60e3).toISOString(),{count}=await db.from('security_events').select('*',{head:true,count:'exact'}).eq('event_type','contact_submit').eq('ip_hash',ipHash).gte('created_at',since);if((count||0)>=5)return out({ok:false,rate_limited:true},429);const body=await req.json();if(body.website)return out({ok:true});const nome=String(body.nome||'').trim(),email=String(body.email||'').trim(),mensagem=String(body.mensagem||'').trim(),tipo=String(body.tipo||'').trim();if(!nome||!email||!mensagem||nome.length>120||email.length>254||mensagem.length>3000)return out({ok:false,error:'Dados inválidos.'},400);const {error}=await db.from('mensagens').insert({nome,email,tipo,mensagem});await db.from('security_events').insert({event_type:'contact_submit',outcome:error?'failure':'success',ip_hash:ipHash});if(error)return out({ok:false,error:'Não foi possível guardar.'},500);return out({ok:true,saved:true,notifications:{photographer:false,client:false}})});
